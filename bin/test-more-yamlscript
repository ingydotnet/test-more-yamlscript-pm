#!/usr/bin/env perl

use strict;
use warnings;

use YAMLScript::Common;
use YAMLScript::RT;

my $file = shift;

YAMLScript::RT->init;
YAMLScript::RT->rep(qq<
  (use 'Test.More.YAMLScript)
  (eval (read-file-ys "$file"))
>);
