#!/usr/bin/env perl

use strict;
use warnings;

use YAMLScript 0.1.0;
use YAMLScript::Main;

my $file = shift;

YAMLScript::Main->new->init->rep(qq<
  (use 'Test.More.YAMLScript)
  (eval (read-file-ys "$file"))
>);
