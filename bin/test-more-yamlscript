#!/usr/bin/env perl

use strict;
use warnings;

use YAMLScript 0.1.0;
use YAMLScript::Main;
use Lingy::Common;

my $file = shift;

RT->init;
RT->rep(qq<
  (use 'Test.More.YAMLScript)
  (eval (read-file-ys "$file"))
>);
